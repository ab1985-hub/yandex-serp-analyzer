Нужно скорректировать логику Wordstat-режима.

Сейчас проблема не в недостаточной фильтрации, а в том, что итоговая семантика не совпадает с тем, что реально показывает Wordstat.

Для режима Wordstat API логика должна быть такой:

1. Итоговый список ключей должен максимально совпадать с Wordstat.
2. Не нужно накладывать поверх Wordstat собственную “умную” тематическую фильтрацию по недвижимости.
3. Не нужно пытаться вручную улучшать семантику, если режим называется именно Wordstat API.
4. В таблице должны показываться те запросы, которые реально вернул Wordstat `GetTop` по заданной фразе и региону, максимально близко к вкладке «Популярные».

Что нужно сделать:

1. Проверьте, не смешиваются ли сейчас лишним образом:

* `results`
* `associations`

Если смешиваются — для основной таблицы Wordstat показывайте только тот набор, который соответствует основным популярным запросам Wordstat.

2. Уберите самодельную тематическую фильтрацию поверх Wordstat-результатов.
   Допустимы только:

* удаление дублей,
* удаление пустых строк,
* применение пользовательских минус-фраз.

3. Логика должна быть такой:

* seed-key
* регион
* запрос в Wordstat
* вывод списка как можно ближе к Wordstat
* пользователь сам отмечает нужные ключи чекбоксами
* выбранные ключи отправляются в анализ

4. Проверьте на кейсе:
   `квартиры для семей с ребенком`
   регион: Москва и область

Нужно, чтобы итоговый список был близок к Wordstat, как в интерфейсе Яндекса:

* квартиры для семей с ребенком
* продажа квартиры для семей с детьми
* квартира для семьи с двумя детьми
* квартиры для семьи с 2 детьми
* дизайны квартир для семьи с ребенком
* двухкомнатная квартира для семьи с ребенком
* однокомнатная квартира для семьи с ребенком
* планировка квартиры для семьи с ребенком
  и т.д.

Если такие фразы реально есть в Wordstat, их оставляем.
Даже если они неидеальны с точки зрения нашей логики.

5. После исправления покажите:

* какие именно поля ответа Wordstat теперь используются;
* не подмешиваются ли associations;
* итоговый список по примеру `квартиры для семей с ребенком`.
